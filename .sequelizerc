const { resolve } = require("node:path");

module.exports = {
  config: resolve("./database/config.js"),
  "models-path": resolve("./src/shared/infra/database/models"),
  "seeders-path": resolve("./database/seeders"),
  "migrations-path": resolve("./migrations"),
};
